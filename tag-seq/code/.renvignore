# Excluded from renv dependency discovery.
#
# These three notebooks are not part of this project's analysis: they are
# copied wholesale from an oyster ploidy x multistressor experiment and read
# inputs that do not exist here. Scanning them would pin a large dependency
# tree (topGO, rrvgo, simplifyEnrichment, WGCNA, ComplexHeatmap, magick) for
# code that cannot run, and 3_ additionally requests "kmisc", which is not a
# CRAN package and would fail the restore outright.
#
# Phase 3 of the reproducibility plan rewrites 3_generate_gene_tables against
# the two tissues that actually exist and deletes the two X_ notebooks. Drop
# the rewritten file from this list at that point so its deps get pinned.

3_generate_gene_tables-salmon.Rmd
X_GOterm_Annotation.Rmd
X_GOSeq_annotation.Rmd
