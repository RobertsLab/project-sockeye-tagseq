# ---- R / RStudio session state ----------------------------------------------
# None of this describes the analysis; it describes one person's editor.
.Rproj.user/
.Rhistory
.RData
.Ruserdata

# ---- Knit / render caches ---------------------------------------------------
*_cache/
*_files/
.quarto/
_freeze/
/docs/site_libs/
# Rendered notebooks land beside their source, or at the project root when
# --output is used, until phase 5 sets up docs/.
tag-seq/code/*.html
/*.html

# ---- renv -------------------------------------------------------------------
# renv.lock IS tracked; the library it installs is not.
renv/library/
renv/staging/
renv/local/
renv/python/
renv/sandbox/

# ---- OS ---------------------------------------------------------------------
.DS_Store

/.quarto/
